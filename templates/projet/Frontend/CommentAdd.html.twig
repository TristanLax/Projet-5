{# templates/projet/Frontent/CommentAdd.html.twig #}
{% extends 'base.html.twig' %}


{% block title %} Ecrire un commentaire {% endblock %}


{% block body %}

<div class="row">
    <div class="col-lg-12">

        <h2 class="text-center comment-main">Répondre au sujet "{{ post.title }}" :</h2>
        {{ form_start(form) }}

        <div class="col-lg-12">{{ form_row(form.content) }}</div>

        {{ form_widget(form) }}
        <div class="col-lg-12">
            <button class="btn btn-secondary">Envoyer le commentaire</button>
        </div>
        {{ form_end(form) }}

    </div>
</div>



{% endblock %}